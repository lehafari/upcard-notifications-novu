version: '3.8'
services:
  mongodb:
    image: mongo:5.0
    container_name: novu-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    
  redis:
    image: redis:7-alpine
    container_name: novu-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
